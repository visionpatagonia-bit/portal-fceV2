/*
 * DEPRECADO. El frontend se reescribio componentizado con ES modules.
 * Punto de entrada actual: /src/app.js (cargado por index.html como type="module").
 * Estructura: /src/{api,store,telemetry,router,format}.js, /src/components/*, /src/views/*.
 * Este archivo ya no se carga y se deja solo para referencia historica.
 */
