# Imágenes de consignas (#16)

Poné acá las imágenes (tabla/cuadro/esquema) de los parciales reales.
Referencialas desde el contrato con: `"image": "/img/<archivo>.png"` en el item de la variante
(o `block.image` para una imagen fija del bloque). Render automático en Evaluar y Devolución.
