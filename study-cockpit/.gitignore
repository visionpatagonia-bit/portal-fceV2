# Runtime / telemetría local (no va al repo)
data/runtime/*.jsonl
data/runtime/*.log
data/runtime/*.secrets.json
*.secrets.json

# Secrets / entorno
.env
.env.*
!.env.example

# Dependencias (este backend no usa deps externas, pero por las dudas)
node_modules/

# OS / editores
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
